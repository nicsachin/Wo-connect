import IconAnalytics from "./IconAnalytics";
import IconCheckbox from "./IconCheckbox";
import IconCamera from "./IconCamera";
import IconEdit from "./IconEdit";
import IconSettings from "./IconSettings";
import IconTask from "./IconTask";
import IconTicketing from "./IconTicketing";
import IconLive from "./IconLive";
import IconDVR from "./IconDVR";
import IconGlobe from "./IconGlobe";
import IconLocation from "./IconLocation";
import IconMap from "./IconMap";
import IconGroup from "./IconGroup";
import IconCalender from "./IconCalender";
import IconTAT from "./IconTAT";
import IconLogout from "./IconLogout";
import IconAddCircle from "./IconAddCircle";
import IconPlayback from "./IconPlayback";
import IconEmployee from "./IconEmployee";
import IconHome from "./IconHome";
import IconMytask from "./IconMytask";
import IconScheduleTask from "./IconScheduleTask";
import IconTicketBold from "./IconTicketBold";
import IconEvent from "./IconEvent";
export function getIcon(name) {
  if (name === "IconAnalytics") return <IconAnalytics />;
  if (name === "IconCheckbox") return <IconCheckbox />;
  if (name === "IconCamera") return <IconCamera />;
  if (name === "IconEdit") return <IconEdit />;
  if (name === "IconSettings") return <IconSettings />;
  if (name === "IconTask") return <IconTask />;
  if (name === "IconTicketing") return <IconTicketing />;
  if (name === "IconLive") return <IconLive />;
  if (name === "IconDVR") return <IconDVR />;
  if (name === "IconGlobe") return <IconGlobe />;
  if (name === "IconLocation") return <IconLocation />;
  if (name === "IconMap") return <IconMap />;
  if (name === "IconGroup") return <IconGroup />;
  if (name === "IconCalender") return <IconCalender />;
  if (name === "IconTAT") return <IconTAT />;
  if (name === "IconLogout") return <IconLogout />;
  if (name === "IconAddCircle") return <IconAddCircle />;
  if (name === "IconPlayback") return <IconPlayback />;
  if (name === "IconEmployee") return <IconEmployee />;
  if (name === "IconHome") return <IconHome />;
  if (name === "IconMytask") return <IconMytask />;
  if (name === "IconScheduleTask") return <IconScheduleTask />;
  if (name === "IconTicketBold") return <IconTicketBold />;
  if (name === "IconEvent") return <IconEvent />;
}

export {
  IconAnalytics,
  IconCheckbox,
  IconCamera,
  IconEdit,
  IconSettings,
  IconTask,
  IconTicketing,
  IconLive,
  IconDVR,
  IconGlobe,
  IconLocation,
  IconMap,
  IconGroup,
  IconCalender,
  IconTAT,
  IconLogout,
  IconAddCircle,
  IconPlayback,
  IconEmployee,
  IconHome,
  IconMytask,
  IconScheduleTask,
  IconTicketBold,
  IconEvent,
};
